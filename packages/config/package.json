{
  "name": "@tteeka/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared configuration package placeholder",
  "engines": {
    "node": ">=24 <25"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "node --test",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  }
}
